# USB lifecycle

Enumeration, configuration, startup, shutdown and recovery appear in checkpoints and captures with different scopes. The interval immediately after connection does not fully show SET_ADDRESS or the electrical connection event. Startup and shutdown sequences must not be presented as a universal contract without the corresponding evidence.
